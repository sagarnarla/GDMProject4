ALL the Codes are written in R.

R Libraries needed: 'igraph'
Install the library by typing:
install.packages('igraph')

The path of the input graph (static.network) will have to modified to point to the appropriate location, in order to run the code.

